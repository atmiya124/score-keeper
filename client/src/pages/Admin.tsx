import { AdminPanel } from "@/components/AdminPanel";

/** Admin page (/admin) – renders the AdminPanel component (match list + create/edit/delete). */
export default function Admin() {
  return <AdminPanel />;
}
