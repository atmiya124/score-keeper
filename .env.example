# Database (required for server and drizzle-kit)
# Use your Neon (or Vercel Postgres / Supabase) URL. Database name must exist (e.g. neondb).
# Do NOT use database "scorekeeper" — that was an old local name and does not exist.
# On Vercel: set in Project Settings → Environment Variables
DATABASE_URL=postgresql://user:password@host:5432/neondb?sslmode=require

# Server (optional; Vercel ignores PORT)
PORT=5000

# Optional: set in production (Vercel sets NODE_ENV=production automatically)
# NODE_ENV=production
